import { inject, TestBed } from '@angular/core/testing';

describe('CompleterService', () => {

    beforeEach(() => {
        TestBed.configureTestingModule({

        });
    });

    // it('should create local data');

});
